type SubJob {
  # Example field (placeholder)
  exampleField: Int
}

input CreateSubJobInput {
  # Example field (placeholder)
  exampleField: Int
}

input UpdateSubJobInput {
  id: Int!
}

type Query {
  subJobs: [SubJob]!
  subJob(id: Int!): SubJob
}

type Mutation {
  createSubJob(createSubJobInput: CreateSubJobInput!): SubJob!
  updateSubJob(updateSubJobInput: UpdateSubJobInput!): SubJob!
  removeSubJob(id: Int!): SubJob
}
